import { describe, expect, it } from 'vitest';
import { part1 } from '.';

describe('Day 15', () => {
  describe('Part 1', () => {
    it('should', () => {
      expect.assertions(1);

      expect(part1()).toStrictEqual(0);
    });
  });
});
